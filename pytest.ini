[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast, no I/O, no external calls
    integration: mocked LLM, tests component interactions
    live: hits real OpenAI API — requires OPENAI_API_KEY, run with -m live
